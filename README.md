# telegram-html-to-md

Simple CLI tool to convert Telegram `messages.html` into individual Markdown files (one post per file).

## Requirements
- Python 3.8+
- Telegram `messages.html` export

> **Note:** It is recommended to place `messages.html` and the tool files in the same project directory.

## Installation
```bash
pip install -r requirements.txt
